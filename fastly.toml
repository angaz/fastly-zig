# This file describes a Fastly Compute package. To learn more visit:
# https://developer.fastly.com/reference/fastly-toml/

authors = [""]
description = ""
language = "other"
manifest_version = 3
name = "fastly-zig"
service_id = ""

# [scripts]
# build = "zig build -Doptimize=ReleaseSmall && mkdir -p bin && cp zig-out/bin/*.wasm bin/main.wasm"

[local_server.backends.example]
url = "https://example.com"

[local_server.backends.weather]
url = "https://wttr.in"
